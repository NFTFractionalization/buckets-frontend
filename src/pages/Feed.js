import {useMoralis, useMoralisWeb3Api, useMoralisWeb3ApiCall} from "react-moralis";

function Feed(props) {

    // const { fetch, data, error , isLoading, } = useMoralisWeb3ApiCall(useMoralisWeb3Api().token.);

    return (
        <div className="bg-black">
            <h2> Feed </h2>
                <div></div>
            <div className="grid grid-cols-2 gap-3">
                <div>
                </div>
            </div>
        </div>);
}
export default Feed;